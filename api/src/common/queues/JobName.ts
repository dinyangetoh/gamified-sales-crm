export enum IngestionJobName {
  RAW_EVENT = 'raw-event',
}

export enum NotificationJobName {
  BADGE_UNLOCK = 'BADGE_UNLOCK',
  STREAK_RISK = 'STREAK_RISK',
  LEVEL_UP = 'LEVEL_UP',
  STREAK_BROKEN = 'STREAK_BROKEN',
}

export enum ScheduledJobName {
  STREAK_RISK_CHECK = 'streak-risk-check',
}
